# Root .gitignore has examples/*/*.csv to drop moldscript outputs;
# undo that here so the curated dataset is tracked.
!substrates.csv

gaussian/opt/*.log
gaussian/opt/*.chk
gaussian/neutral/
gaussian/oxidized/
gaussian/reduced/
results/
MOLDSCRIPT_*.dat
targets.json
__pycache__/
