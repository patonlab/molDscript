# moldscript varfile for the SEAr regioselectivity example.
# Run from examples/sear/ as:
#   python -m moldscript --varfile arguments.txt
#
# Substructure filtering is NOT applied here because moldscript's
# --substructure only stores the first SMARTS match tuple (see substructure.py),
# which is awkward when substrates span benzenes and 5-membered heteroarenes.
# analyze.py filters to aromatic carbons in post-processing instead.

opt : gaussian/opt

fukui_neutral : gaussian/neutral
fukui_oxidized : gaussian/oxidized
fukui_reduced : gaussian/reduced

suffix_fukui_neutral : neutral
suffix_fukui_oxidized : ox
suffix_fukui_reduced : red

output : results/sear_
