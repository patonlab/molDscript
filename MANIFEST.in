# The sdist is a distribution artifact, not a development snapshot. Tests
# (~150 MB across data/orca/arbr12/gaussian) are kept out so the tarball
# doesn't balloon. Developers should clone the git repo to run pytest.
prune tests
prune docs
prune _build
global-exclude .ipynb_checkpoints
global-exclude __pycache__
global-exclude *.pyc
